# Контекст проекта: RatioT SCADA

## Предустановки ассистента

- Имя: **Кимико**
- Род: женский
- Язык работы: **только русский**
- Язык мышления и рассуждений: **только русский**
- Рабочая папка документации: `C:/repos/doc/scada-doc`
- Папка с кодом проекта: будет отдельно (уточнить путь позже)
- Принцип: не врать. Если не знаю — сказать. Если не уверена — сказать. Если ошиблась — исправить.
- **Жёсткое требование пользователя:** мыслить, рассуждать и общаться **только на русском**. Это обязательное правило, нарушать нельзя.

## Структура рабочих папок

- `C:/repos/doc/` — проект документации RatioT SCADA.
  - `scada-doc/` — исходные материалы: PDF-руководства, извлечённые тексты, локальный сайт (`site/`), `KIMIKO_CONTEXT.md`.
  - `github-ratiot-scada-doc/` — GitHub-зеркало репозитория, публикуется на `https://noo001.github.io/ratiot-scada-doc/`.
  - `ratiot-scada-doc-publish/` — GitLab Pages-версия.
  - `ratiot-doc-wiki/` — GitLab Wiki.
- `C:/repos/lib/` — библиотеки и портативные окружения.
  - `python-embed/` — портативный Python с установленными зависимостями (используется для генерации PDF).

## Текущее состояние проекта

- В папке `incoming/` находятся два PDF-файла:
  - `Руководство по развертыванию RatioT SCADA.pdf`
  - `Руководство по эксплуатации RatioT SCADA.pdf`
- Проект только начинается. Требуется погружение в материалы и формирование архитектуры/плана.

## Цели (предварительные, требуют уточнения)

1. Разобрать существующую документацию по RatioT SCADA.
2. Понять систему, её компоненты, процессы развёртывания и эксплуатации.
3. Подготовить структуру документации / базу знаний / техническое задание.
4. Возможно, разработать отдельное приложение/инструмент (папка с кодом будет позже).

## Что уже сделано

- Извлечён текст из обоих PDF в `extracted/`.
- Проанализировано «Руководство по развёртыванию»; собрана сводка по архитектуре, стеку, портам, установке.
- Определена структура «Руководства по эксплуатации» (протоколы, UI/HMI, скрипты, DevOps).
- Создан локальный веб-документ «RatioT SCADA — база знаний»:
  - `site/index.html` — общая сводка
  - `site/ux.html` — выжимка по пользовательскому опыту
  - `site/style.css`
  - `server.pl` — минимальный локальный сервер
- Запущен локальный сервер: **http://localhost:8765/site/**


## Извлечённые данные по RatioT SCADA

### Общая характеристика

- **RatioT SCADA** — SCADA/HMI-платформа, серверная часть работает на Java.
- Серверное ПО устанавливается как служба/демон на Windows (7/10/11/Server 2016/2019) и Linux.
- Клиентская часть — Web UI на React; запускается в браузерах Google Chrome / Mozilla Firefox.
- Есть отдельный инструмент **Net Admin** на порту **6440**.

### Компоненты и технологический стек

| Компонент | Описание |
|-----------|----------|
| JVM | Java 17; настройки через `*.vmoptions` (`-Xms/-Xmx`, прокси, язык) |
| БД конфигураций / исторические данные | Apache Cassandra (NoSQL), встроенная; опционально — RDBMS: MySQL, PostgreSQL, Oracle, MS SQL Server, Firebird |
| Встроенные БД | H2 / Cassandra для хранения |
| API | Собственный бинарный/API-протокол на порту **6460** |
| Web UI | HTTP **8080** / HTTPS **8443** (`/web`) |
| Net Admin | порт **6440** |
| SNMP | **162/UDP** |
| Мониторинг потоков | NetFlow **2055**, sFlow **6343**, Syslog **514** |
| Дополнительно | FTP/SSH/TFTP, BACnet **47808**, DNS, LDAP/AD, RADIUS, SMTP/POP3/IMAP, Heartbeat **7800** |

### Установка и развёртывание

- Дистрибутивы:
  - Windows: `RatioT_SCADA_full_6.34.07_windows-x64.exe`
  - Linux: `RatioT_SCADA_full_6.34.07_unix-x64.sh`
- Режимы установки:
  - GUI (`installer -c`)
  - Тихая (`installer -q -varfile response.varfile`)
  - Вручную через `response.varfile`
  - Docker-образ (Dockerfile с `xvfb-run --auto-servernum ./ag_server -r`)
- Целевой каталог по умолчанию:
  - Linux: `/opt/RatioT SCADA`
  - Windows: `C:\Program Files\RatioT SCADA`
- Лицензирование: `activation.txt` → получение `server.license`, активация через Net Admin.

### Управление сервером

- Linux:
  - systemd: `systemctl enable|restart|status %LS_BINARY%.service`
  - init-скрипт: `service %LS_BINARY%_service start|stop|restart|status`
  - консоль: `%LS_BINARY%_console`
- Windows: служба + консоль `%LS_BINARY%_console.exe`.

### Ключевые сущности системы

- **Проект (.prs)** — основная единица конфигурации SCADA.
- **Server / LinkServer** — ядро сбора данных, обработки тегов, хранения.
- **Модель (Model)** — логическое представление объекта/устройства.
- **Устройство (Device)** — источник данных (RTU/PLC/контроллер).
- **Тег / переменная** — значение из устройства или виртуальное.
- **Историк (Historian)** — хранение временных рядов.
- **Dashboard / HMI** — визуализация: формы, SVG-графика, таблицы, диаграммы.
- **События и алармы** — журналирование, SLA, уведомления (e-mail/SMS).
- **Пользователи и роли** — аутентификация, права доступа.
- **SQL-запросы** — встроенная возможность выполнять запросы к данным системы и внешним БД.
- **Скрипты / выражения** — поддержка Java, R, Python; DSL для binding вида `{users.admin.devices...}`.

### Поддерживаемые протоколы и источники данных (по оглавлению «Руководства по эксплуатации»)

- Modbus (TCP/UDP/RTU/ASCII/Serial)
- OPC (DA) / OPC UA
- IEC-104 / IEC-104 Server
- DNP3
- MQTT
- SNMP
- HTTP/HTTPS
- BACnet
- CoAP
- DLMS/COSEM
- Ethernet/IP
- Kafka
- M-Bus
- NMEA 0183
- Omron FINS
- Siemens S7
- SIP
- SMPP
- SOAP
- SQL-подключения (JDBC)
- VMware
- WebSphere MQ
- WMI
- XMPP
- CORBA
- CWMP
- FTP/SSH/TFTP/SMB/CIFS
- Asterisk
- IPMI
- JMX
- Message stream
- RADIUS / LDAP / Active Directory

### Структура «Руководства по эксплуатации»

- 2849 страниц, ~114 600 строк извлечённого текста.
- Включает: введение, установку и администрирование, описание модели данных, протоколов, UI/HMI, скриптов, DevOps, резервное копирование, безопасность, API, примеры.
- Текст успешно извлечён и сохранён в `extracted/эксплуатация_utf8.txt`.

### Структура «Руководства по развёртыванию»

- 98 страниц, ~2600 строк.
- Основные разделы: требования, установка, лицензирование, запуск, архитектура SCADA/HMI, базовые примеры dashboard, события/SLA, резервное копирование, обновление/удаление.
- Текст сохранён в `extracted/развертывание_utf8.txt`.

## GitLab-проект для документации

- Проект: `https://gitlab.dkc.ru/ratiot/doc` (переименован из `ratiot/ratiot-scada-doc`).
- Текущая роль пользователя: **Maintainer/Owner** (права получены, проект переименован, настройки Pages изменены).
- В репозиторий запушена структура сайта:
  - `index.html` — главная страница с плитками проектов.
  - `style.css` — общие стили.
  - `scada/index.html` — база знаний по RatioT SCADA.
    - Добавлены разделы: подключение устройств (Modbus/OPC UA/MQTT), теги/переменные, дашборды, историк/архивы, алармы/события, пользователи/права, резервное копирование/обновление, API/интеграции.
  - `scada/ux.html` — **расширенное руководство пользователя** (устройства, модели, дашборды, binding, SQL, аварии, доступ).
  - `ux.html` — корневой редирект на `scada/ux.html`.
  - `.gitlab-ci.yml` — job `pages` для GitLab Pages с тегом `shell`.
  - `README.md` — справка по запуску, структуре, локальной разработке и добавлению новых проектов.
- Локальная рабочая копия: `C:/repos/doc/ratiot-scada-doc-publish`.
- Последний push: `b9c4b99` — добавлен README.md с инструкциями.
- **Pipeline:** `2490` — **success** (job `pages` прошёл за 58 секунд, runner `tvr-v-rdep` взял задачу). Повторный pipeline `2491` (после README) — тоже **success**.
- **GitLab Pages:**
  - `pages_access_level` изменён на `public` через API.
  - Возможные URL (требуется проверка в UI): `https://gitlab.dkc.ru/ratiot/doc/-/pages` или `https://gitlab.dkc.ru/pages/ratiot/doc/`.
  - Пока без сессии GitLab редиректит на sign_in — нужно уточнить правильный URL Pages в настройках проекта.
- **Старый локальный сервер:** остановлен. `perl server.pl` больше не работает, документация публикуется через GitLab Pages.
- **GitHub-зеркало:**
  - Репозиторий: `https://github.com/Noo001/ratiot-scada-doc`
  - Локальная копия: `C:/repos/doc/github-ratiot-scada-doc`
  - Workflow: `.github/workflows/pages.yml`
  - URL Pages: `https://noo001.github.io/ratiot-scada-doc/`
  - Публичная часть SSH-ключа (`/c/Users/andrey.efremtsev/.ssh/ratiot_scada_doc_deploy.pub`):
    ```text
    ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKoNsvYSP4qd9XblAMqZ5URYnZCnxWSJmd7DOTxyZKAZ deploy@ratiot-scada-doc
    ```
  - Ключ добавлен как deploy key в GitHub-репозиторий.
  - Содержимое синхронизировано с GitLab: `scada/index.html` и `ux.html` идентичны.
  - Добавлена отчётная страница `bug_cases_report.html` для руководителя:
    - Группировка по гуглдоку: «Предъявленные Объединению», «Новые баги», «Дополнительно», «Исключены».
    - Вверху — кнопка «Скачать PDF» (`RatioT_SCADA_Bug_Cases.pdf`).
    - В начале — сводная таблица вместо оглавления.
- **GitLab Wiki:**
  - Репозиторий вики: `git@gitlab.dkc.ru:ratiot/doc.wiki.git`
  - Локальная копия: `C:/repos/doc/ratiot-doc-wiki`
  - Созданы страницы:
    - `home.md` — полная Markdown-версия базы знаний RatioT SCADA (752 строки).
    - `ux.md` — руководство пользователя по интерфейсу (637 строк).
  - В `home.md` есть оглавление и ссылка на `ux.md`.
  - Wiki доступна по URL: `https://gitlab.dkc.ru/ratiot/doc/-/wikis/home` (требуется авторизация в GitLab).
- **Следующий шаг:** включить GitHub Pages в настройках GitHub-репозитория; решить вопрос с DNS/доменом для GitLab Pages.

## Последние доработки отчёта по багам (GitHub-зеркало)

- **Левая колонка с навигацией:** в `bug_cases_report.html` добавлен `<aside>` со ссылками на группы разделов и на каждый кейс. Реализовано через `rebuild_report.py`.
- **Подсветка активного пункта при скролле:** добавлен `IntersectionObserver`, который выделяет текущий раздел/кейс в боковой навигации (класс `.active`).
- **Адаптация вёрстки:** добавлены `overflow-wrap`, `word-break` и `min-width: 0` для корректного отображения длинных ссылок и таблиц в двухколоночном layout.
- **Замена терминологии:** во всём отчёте слово «Серьёзность» заменено на «Критичность» (заголовки таблицы, мета-информация кейсов, парсинг `tests/BUG_CASES.md`).
- **Изменение критичности кейса:** кейс «Ошибки создания ресурсов при первом запуске сервера» переведён с Medium на Low.
- **Генерация артефактов:** HTML-отчёт и PDF перегенерируются скриптами `rebuild_report.py` + `generate_pdf.py` (Playwright + Chromium).
- **Файлы:** `RatioT_SCADA_Bug_Cases.pdf`, `bug_cases_report.html`, `rebuild_report.py`, `tests/BUG_CASES.md` синхронизированы и запушены в `main` на GitHub.
- **URL для проверки:** https://noo001.github.io/ratiot-scada-doc/bug_cases_report.html

## Безопасность Web UI при выносе наружу

- Web UI доступен по `https://<host>:8443/web` (HTTPS) и `http://<host>:8080/web` (HTTP, если включён незащищённый доступ).
- **Порт 8080 (HTTP) наружу открывать нельзя** — трафик не шифруется.
- **Порт 6440 (Net Admin)** — административный интерфейс, должен быть доступен только внутри локальной сети/VPN.
- **Стандартные учётные данные `admin / admin`** — первое, что нужно сменить после установки.
- **Управление пользователями и паролями доступно:** можно создавать пользователей, роли, ограничивать права на уровне переменных/контекстов/дашбордов.
- **Поддерживается внешняя аутентификация:** LDAP/Active Directory, RADIUS, OAuth 2.0 / OpenID Connect.
- **Рекомендация:** не выставлять SCADA напрямую в интернет, использовать VPN/firewall, оставить наружу только 8443/HTTPS, сменить стандартный пароль, ограничить права пользователей.

## Открытые вопросы

- Какая конечная цель проекта? (документация, генератор документации, портал, другое)
- Кто конечные пользователи?
- Есть ли сроки?
- Кто ещё в команде?
- Где будет располагаться папка с кодом?

## Правила ведения файла

- Это живой документ. Обновляется по мере поступления информации.
- В него записываются только проверенные и важные тезисы.
- Все договорённости фиксируются здесь.
